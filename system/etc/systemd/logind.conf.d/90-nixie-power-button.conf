[Login]
# Let Hyprland distinguish a sleeping display from an awake session and show
# the confirmation UI. A firmware-level emergency hold cannot be intercepted.
HandlePowerKey=ignore
HandlePowerKeyLongPress=ignore
